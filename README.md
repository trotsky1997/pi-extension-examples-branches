# pi-extension-examples-branches

Each branch in this repository contains exactly one installable pi extension package.

Install a branch with:

```bash
pi install git:github.com/trotsky1997/pi-extension-examples-branches@<branch>
```
